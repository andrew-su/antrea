apiVersion: v1
kind: Service
metadata:
  name: antrea-idps
  namespace: {{ .Release.Namespace }}
  labels:
    app: antrea-idps
spec:
  ports:
    - port: 443
      protocol: TCP
      targetPort: api
  selector:
    app: antrea-idps
    component: antrea-idps-controller
