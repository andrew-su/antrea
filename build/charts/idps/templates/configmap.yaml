apiVersion: v1
kind: ConfigMap
metadata:
  name: antrea-idps-config
  namespace: {{ .Release.Namespace }}
  labels:
    app: antrea-idps
data:
{{ tpl (.Files.Glob "conf/*").AsConfig . | indent 2 | replace "  \n" "\n" }}
